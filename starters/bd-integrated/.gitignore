# Building framework — local read-only dependency (symlink or nested clone)
building

# macOS
.DS_Store

# Beads / Dolt files are added automatically by `bd init` (.dolt/, *.db, .beads-credential-key).
# Do not duplicate them here.
